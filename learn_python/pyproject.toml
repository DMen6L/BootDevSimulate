[project]
name = "learn-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DMen6L", email = "240103073@sdu.edu.kz" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "pytest>=9.1.1",
]

[project.scripts]
learn-python = "learn_python:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
